spring:
  datasource:
    url: jdbc:h2:mem:ziovpo_labi_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

server:
  ssl:
    enabled: false

jwt:
  secret: test-secret-value-with-at-least-thirty-two-characters
  access-token-expiration-ms: 900000
  refresh-token-expiration-ms: 604800000
